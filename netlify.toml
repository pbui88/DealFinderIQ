[build]
  command   = "npm run build"
  publish   = "dist"
  functions = "netlify/functions"

[dev]
  command    = "npm run dev"
  port       = 8888
  targetPort = 5173

[functions]
  node_bundler = "esbuild"

[functions."generate-points"]
  timeout = 20

[functions."admin"]
  timeout = 26

[functions."geocode-points"]
  timeout = 26

[functions."scheduled-skip-trace-check"]
  schedule = "*/5 * * * *"

[functions."scheduled-zip-backfill-background"]
  schedule = "*/5 * * * *"


[[headers]]
  for = "/*"
  [headers.values]
    X-Frame-Options = "DENY"
    X-Content-Type-Options = "nosniff"
    Referrer-Policy = "strict-origin-when-cross-origin"
    Strict-Transport-Security = "max-age=63072000; includeSubDomains; preload"
    Permissions-Policy = "camera=(), microphone=(), geolocation=()"
